export interface ILoginData {
  email: string;
  password: string;
}

export interface IsignupData {
  name: string;
  email: string;
  password: string;
  confirmpassword: string;
  provider: boolean;
}
